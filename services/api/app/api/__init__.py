"""The FastAPI surface for InsightGPT — routers, auth wiring, middleware.

Import the ASGI app from :mod:`app.api.main` (run via
``uvicorn app.api.main:app``). See ``docs/06-api.md`` for the contract.
"""
